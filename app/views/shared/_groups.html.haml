.section.half
  %h3 Groups
  - unless resource.node_group_list.empty?
    %table.inspector
      %thead
        %tr
          %th Group
          %th Source
      %tbody
        - resource.node_group_list.each do |group,parents|
          %tr
            %td
              %strong= link_to(group.name,group)
            %td
              - if parents.include?(resource)
                = resource.name
              - else
                = parents.map{|p| link_to(p.name,p)}.join(", ")
  - else
    = describe_no_matches_as 'No groups'
