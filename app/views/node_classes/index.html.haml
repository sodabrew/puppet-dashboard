#sidebar= render 'shared/node_manager_sidebar'
#main
  .header
    %h2.half Classes
    = render 'shared/search' if action_name == "index"
    %div{:style => 'clear:both;'}

  %table.main
    %thead
      %tr
        %th.check
          = check_box_tag "check_all"
        %th.name
          Name
    %tbody
      - @node_classes.each do |n|
        %tr[n]
          %td.check
            = check_box_tag "check_all"
          %td.name
            = link_to h(n.name), n
  = pagination_for @node_classes
