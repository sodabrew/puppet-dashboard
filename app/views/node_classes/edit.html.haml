#sidebar= render :partial => 'shared/node_manager_sidebar'
#main
  = form_for @node_class, :url => node_class_path(@node_class.id) do |f|
    = render f
    .element.actions
      %button Update
      or
      = link_to 'Cancel', @node_class
