%span{:title => report ? report_title_text(report) : 'No reports'}
  - if report
    - if report.kind == "inspect"
      = icon(:inspect)
    - else
      = icon(report.status)
  - else
    = icon(:error)
