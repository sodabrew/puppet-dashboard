= javascript_include_tag 'validation_response', :cache => 'validation'
#sidebar= render 'shared/node_manager_sidebar'
#main
  - remote_form_for @node_group, :url => url_for(@node_group), :success => "form_submit_success(request.responseJSON, 'error_messages')", :failure => "alert(\'Oops!! An error occurred.\')", :html => {:id => "param_form"} do |f|
    %input{:id => "force_update", :name => "force_update", :type => "hidden", :value => "false"}
    = render f
    .element.actions
      %button{:id => "submit_button"} Update
      or
      = link_to 'Cancel', @node_group
